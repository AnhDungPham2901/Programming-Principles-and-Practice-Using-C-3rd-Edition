#include "../../PPPHeaders.h"

int main() {
    int area(int length, int width);

    // int x0 = arena(7,2); // error message: arena was not declared in this cope; did you mean 'area'?
    // int x1 = area(7); // error message: too few arguments to function 'int area(int, int)
    // int x2 = area("seven", 2); // error message: invalid conversion from 'const char*' to 'int'
    // int x3 = area(1,2,3); // error message: too many arguments to function
    return 0;
}